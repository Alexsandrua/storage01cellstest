"use server"
import dataload from '../resurce/db';
import temporareStor from '../resurce/rdb'

export async function actionNameOne(data) {
    console.log('DATA ', data)
    //if (!temporareStor.tokens[data.reg.regToken])
       // temporareStor.tokens[`${data.reg.regToken}`] = {};
    //if (data.nameOne)
      //  temporareStor.tokens[data.reg.regToken][data.nameOne] = data.nameOne;
    //if (data.nameTwo)
     //   temporareStor.tokens[data.reg.regToken][data.nameTwo] = data.nameTwo;
    //if (data.nameP)
  //      temporareStor.tokens[data.reg.regToken][data.nameP] = data.nameP;
   // console.log("TemporareStor", '"', temporareStor, '"');
}